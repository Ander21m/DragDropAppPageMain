import 'package:flutter/material.dart';

class Tryonpage extends StatelessWidget {
  const Tryonpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Try On Page"));
  }
}